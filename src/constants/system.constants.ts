const navbarHeight = 64;
const contentPadding = 16;

const SystemConstants = {
   NavbarHeight: navbarHeight,
   ContentPadding: contentPadding,
   ContentHeightCalc: navbarHeight + (contentPadding * 2),
};

export default SystemConstants;
